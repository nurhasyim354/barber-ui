/** Gaya nama outlet untuk halaman publik (QR, invoice) — tidak mengikuti theme tenant. */
export const PUBLIC_TENANT_NAME_SX = {
  color: '#111827',
  fontWeight: 700,
  lineHeight: 1.35,
} as const;

export const PUBLIC_TENANT_SUBTITLE_SX = {
  color: '#374151',
} as const;
